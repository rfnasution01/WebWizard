import { Box } from "@mui/material"
import { MainRouting } from "../../../routes"

export const MainArticle = () => {
    return (
        <Box
            component='article'
        >
            <MainRouting />
        </Box>
    )
}