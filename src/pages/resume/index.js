import { Typography } from "@mui/material"

export const ResumePage = () => {
    return (
        <Typography
            sx={{
                textAlign: 'center'
            }}
        >Resume Page</Typography>
    )
}