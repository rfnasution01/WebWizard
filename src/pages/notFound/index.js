import { Typography } from "@mui/material"

export const NotFoundPage = () => {
    return (
        <Typography
            sx={{
                textAlign: 'center'
            }}
        >Not Found Page</Typography>
    )
}