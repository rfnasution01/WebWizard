import { Typography } from "@mui/material"

export const HomePage = () => {
    return (
        <Typography
            sx={{
                textAlign: 'center'
            }}
        >Home Page</Typography>
    )
}